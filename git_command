[git]

git remote -v
git remote set-url 



git fetch получение изменений с сервера
git pull получение изменений с сервера и
слияние в локальную ветку

git diff изменения

git reset HEAD~ отмена
git status статус

git merge имя_ветки слияние

имя_ветки — ветка, которая будет
объединена с принимающей

git log --oneline --abbrev-commit --all -
-graph
история с псевдографикой

git branch ветки
git checkout Переключение
Чтобы создать ветку и сразу
переключиться на нее, можно выполнить
команду git checkout с параметром -b
git checkout -b имя_ветки

git commit коммит

git commit -a -m "fixed !"

git mv переместить или переименовать
git add . добавление
git init инициализация

git rm удалить файл

git stash stash берет изменённое состояние
вашей рабочей директории, то есть
изменённые отслеживаемые файлы и
проиндексированные изменения, и
сохраняет их в хранилище
незавершённых изменений

